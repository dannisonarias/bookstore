import React from 'react';

const initialState = {
    id:'1',
    title:'Book1',
    category:'Sci-Fi',
}

const bookReducer = ( state = initialState ,action)=>{
    return state;
};
export default bookReducer;